extends _partials/layout

block site-title
    != page.title + " · " + config.title

block description
    - desc = page.desc || page.title + ' - ' + config.author;
    meta(name="description", content=desc)

block container
  if /^categories\//.test(page.path)
    
  else if /^tags\//.test(page.path)
    
  else
    include _mixins/post
    +post(page)

block pagination
  include _mixins/paginator
  +post()
  if page.comments
    include _partials/_components/comment

block information
  include _partials/_components/information
